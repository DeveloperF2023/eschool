import 'package:flutter/material.dart';

class MyExamsByDateScreen extends StatelessWidget {
  const MyExamsByDateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
